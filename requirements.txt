# 财税助手全量依赖（后端运行时 + 数据工具链）
# 安装：pip install -r requirements.txt
# 提示：torch/FlagEmbedding 体积较大；国内网络建议先设 HF_ENDPOINT=https://hf-mirror.com
#
# ── 后端运行时（与 backend/requirements.txt 一致）──
langchain>=1.3.0,<2.0.0
langchain-core>=1.4.0,<2.0.0
langchain-openai>=1.4.0
langgraph>=1.2.0,<1.3.0
langgraph-checkpoint>=2.0.0
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0
python-dotenv>=1.0.0
FlagEmbedding>=1.2.0
torch>=2.0.0
qdrant-client>=1.10.0
numpy>=1.26.0
PyYAML>=6.0
Markdown>=3.8.0
openpyxl>=3.1.0
#
# ── 数据工具链（scripts/：清洗/切分/向量化/评测/MCP）──
beautifulsoup4>=4.13.5    # clean_to_md.py：HTML/MHTML → 干净 Markdown
requests>=2.32.0          # 下载与接口冒烟
markitdown[pdf]>=0.1.6    # 申报表 PDF → Markdown
mcp>=1.0.0,<2.0.0         # tax-calc MCP Server（注意：mcp 2.x 移除 FastMCP，锁定 <2.0）
