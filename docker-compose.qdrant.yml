# 仅 Qdrant 向量数据库（开发环境用）
# 启动: docker compose -f docker-compose.qdrant.yml up -d
version: '3.8'

services:
  qdrant:
    image: qdrant/qdrant:latest
    container_name: finance-rag-qdrant
    ports:
      - "6333:6333"
    volumes:
      - ./qdrant_data:/qdrant/storage
    restart: unless-stopped
