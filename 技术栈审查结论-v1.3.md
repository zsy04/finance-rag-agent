# 个人成长系统 · 技术栈审查结论

> **审查编号**：第六轮（AI 编码全流程优化审查）
> **审查日期**：2025-07-18
> **审查范围**：技术栈全分支深度追问，聚焦「AI 全量编码 + 最小失误 + 最快交付」
> **前序文档**：PRD v1.2 + 原型搭建计划 v1.2（五轮审查后定稿）

---

## 一、审查结论总览

本轮审查围绕一个核心约束展开：**所有代码由 AI 产出，人只做部署环境初始化**。逐分支追问后，确认技术栈主体不变，但有三项关键修订。

### 技术栈（无变更）

```
FastAPI (Python 3.11+) + Vite/React 18（纯 CSR）+ Supabase（单实例）+ shadcn/ui + DeepSeek（BYOK）
```

### 本次修订清单

| # | 修订项 | 原方案 | 修订后 | 影响范围 |
|---|--------|--------|--------|---------|
| **R1** | 数据访问路径 | 读直连 Supabase + 写走 FastAPI | **统一走 FastAPI**，前端只调 `/api/v1/*`，前端 supabase-js 仅用于 Auth | `api.ts`、TanStack Query 全部 queryFn |
| **R2** | 流式对话 | AI 自行推断 ReadableStream | **新增 F4：预写 `useStreamChat` hook 模板 + FastAPI `stream_chat` 端点模板** | 新增 `hooks/useStreamChat.ts`、`routers/chat.py` |
| **R3** | 模板化策略 | 人工预写 25 行（F 档 3 项） | **扩展为 F1-F4 四项模板，全部由 AI 生成，人仅 review** | 新增 `useStreamChat.ts`、`stream_chat` 端点 |
| **R4** | pgmq 休眠 | 未讨论 | 接受 PaaS scale-to-zero 风险，画像更新可延迟半天 | 部署文档加备注 |

---

## 二、数据访问路径：统一走 FastAPI

### 决策理由

原方案「前端 TanStack Query 读 Supabase + 写走 FastAPI」存在 AI 编码分叉陷阱：AI 需判断每次操作"该直连还是走 API"。对 AI 编码而言，分叉路径 = 出错来源。

### 修订方案

```
所有数据 CRUD → FastAPI /api/v1/*
前端 supabase-js → 仅用于 Auth（登录/注册/session/onAuthStateChange）
```

**前端 `api.ts` 统一封装**：

| 维度 | 规格 |
|------|------|
| 暴露方法 | `api.get(path)` / `api.post(path, body)` / `api.put(path, body)` / `api.delete(path)` |
| 基地址 | `VITE_API_BASE_URL` 环境变量 |
| JWT 携带 | 自动从 `supabase.auth.getSession()` 获取 → `Authorization: Bearer <token>` |
| 错误处理 | 非 2xx → throw `ApiError { code, message, detail }` |
| 预估行数 | ~25 行 |

**后端 Supabase 客户端**（F2 模板，已有）：

```python
# backend/auth/supabase_client.py
# 工厂函数：入参 user_token → 返回已注入用户 JWT 的 supabase-py 客户端
# ~15 行
```

### 连锁变更

| 文件 | 变更 |
|------|------|
| `frontend/src/lib/api.ts` | 重写：从 supabase-js CRUD 改为 fetch 封装 |
| `frontend/src/lib/supabase.ts` | 精简：仅保留 Auth 相关（`createClient` + `onAuthStateChange`） |
| 所有 TanStack Query hooks | `queryFn` 从 `supabase.from(...)` 改为 `api.get(...)` |
| 所有 mutation hooks | `mutationFn` 改为 `api.post/put/delete(...)` |

---

## 三、流式对话模板化（新增 F4）

### 决策理由

流式 AI 对话（仪表盘追问 + 日记探讨）是用户体验的核心——打字机效果失败 = 产品感消失。ReadableStream + AbortController 两端代码对 AI 编码属于中高风险，必须模板化。

### 新增模板

#### F4a：前端 `useStreamChat` hook

| 维度 | 规格 |
|------|------|
| **文件** | `frontend/src/hooks/useStreamChat.ts` |
| **输入参数** | `{ apiUrl: string }` |
| **返回值** | `{ messages, isStreaming, sendMessage(text, context), stopGeneration(), error }` |
| **核心逻辑** | ① fetch POST → 获取 ReadableStream ② reader.read() 循环 ③ 逐 chunk 追加到末条 assistant message ④ AbortController 支持停止 ⑤ reader.cancel() 清理 |
| **异常覆盖** | 网络断开 → error 状态 + 可重试；空返回 → 兜底文案；用户停止 → 保留已生成；流中断 → 保留已生成 + 提示 |
| **预估行数** | ~40 行 |

#### F4b：后端 `stream_chat` 端点

| 维度 | 规格 |
|------|------|
| **文件** | `backend/routers/chat.py`（新增） |
| **端点** | `POST /api/v1/chat` |
| **请求体** | `{ messages: [...], context_type: "advice"｜"reflect", context_id?: string }` |
| **响应** | `StreamingResponse(media_type="text/event-stream")` |
| **核心逻辑** | ① get_current_user → user_id ② 读 user_profiles_ai 拼接 system prompt ③ openai SDK async stream ④ async for chunk → yield delta ⑤ 捕获异常 → yield error SSE event |
| **预估行数** | ~30 行 |

### AI 编码简化项总览（F 档完整版）

| 编号 | 风险组件 | 简化方案 | 预估行数 | 信心提升 |
|------|---------|---------|---------|---------|
| F1 | pyjwt 本地验签 | Supabase `GET /auth/v1/user` REST API | 0 行（改调用） | 30% → 95% |
| F2 | supabase-py JWT 透传 | 预写工厂函数模板 | ~15 行 | 30% → 95% |
| F3 | pgmq API 推断 | 预写 `pgmq_helpers.sql` 模板 | ~10 行 | 10% → 95% |
| **F4** | **流式对话** | **预写 `useStreamChat` + `stream_chat` 模板** | **~70 行** | **25% → 90%** |

> 四项模板合计约 **95 行**，全部由 AI 生成，人 review 修正。

---

## 四、pgmq 休眠风险

### 问题

Railway / Render 免费 tier 在无 HTTP 请求 15 分钟后休眠实例，asyncio 后台协程暂停，pgmq 消费中断。

### 风险分析

| 消息类型 | 消费策略 | 休眠影响 |
|---------|---------|---------|
| `reflection` | fire-and-forget 即时消费 | ✅ 不影响（日记保存时立即消费） |
| `profile_update` | 凌晨窗口批量消费 | ⚠️ 可能延迟半天 |

### 决策

**接受风险**。毕业设计非生产环境，画像晚更新几小时不影响核心体验。如需保险：
- Render：用 `cron-job.org` 每 10 分钟 ping `/api/v1/health`（免费）
- Railway：付费 tier 设 Minimum Replicas = 1

---

## 五、部署配置速查

### 平台分工

| 平台 | 部署内容 | 关键配置 |
|------|---------|---------|
| **Vercel** | 前端静态文件（`vite build` 产物） | 3 个环境变量 |
| **Railway / Render** | FastAPI 后端 | 8 个环境变量 + CORS |
| **Supabase** | PostgreSQL + Auth + pgmq | SQL 执行 + RLS |

### 前端环境变量（Vercel）

| 变量 | 说明 |
|------|------|
| `VITE_API_BASE_URL` | 后端地址，如 `https://xxx.railway.app` |
| `VITE_SUPABASE_URL` | Supabase 项目 URL |
| `VITE_SUPABASE_ANON_KEY` | Supabase anon key |

### 后端环境变量（Railway/Render）

| 变量 | 说明 | 生成方式 |
|------|------|---------|
| `SUPABASE_URL` | Supabase 项目 URL | Supabase → Settings → API |
| `SUPABASE_SERVICE_ROLE_KEY` | service_role key（用于 pgmq 消费） | Supabase → Settings → API |
| `AES_ENCRYPTION_KEY` | BYOK Key 加密密钥 | `openssl rand -hex 32` |
| `DEFAULT_AI_PROVIDER` | `deepseek` | 固定值 |
| `DEFAULT_AI_API_KEY` | 系统默认 DeepSeek Key | DeepSeek 控制台 |
| `DEFAULT_AI_MODEL` | `deepseek-chat` | 固定值 |
| `DEFAULT_AI_BASE_URL` | `https://api.deepseek.com/v1` | 固定值 |
| `FRONTEND_URL` | 前端 Vercel 域名 | 部署后填写 |

### CORS 配置（`backend/main.py`）

```python
app.add_middleware(
    CORSMiddleware,
    allow_origins=[os.getenv("FRONTEND_URL"), "http://localhost:5173"],
    allow_credentials=True,
    allow_methods=["*"],
    allow_headers=["*"],
)
```

### 部署检查清单

- [ ] Supabase：`schema.sql` + `pgmq_helpers.sql` 已执行
- [ ] Supabase：七表 RLS 策略已启用
- [ ] Railway/Render：8 个环境变量全设
- [ ] Railway/Render：`/api/v1/health` 返回 200
- [ ] Vercel：3 个环境变量全设
- [ ] Vercel：`VITE_API_BASE_URL` 指向后端（非 localhost）
- [ ] 浏览器：前端登录 → 速记 → AI 建议全链路通

---

## 六、AI 编码风险热力图（修订后）

| 模块 | 风险等级 | 缓解措施 |
|------|---------|---------|
| Auth（登录/注册/session） | 🟢 低 | Supabase Auth UI 组件 + `/auth/v1/user` |
| 前端数据访问 | 🟢 低 | 统一走 `api.ts` 封装 |
| 表单（速记/日记/设置） | 🟢 低 | React Hook Form + Zod |
| pgmq 消费 | 🟢 低 | 模板 `pgmq_helpers.sql` |
| JWT 验证 | 🟢 低 | Supabase REST API |
| 流式对话前端 | 🟢 低 | 模板 `useStreamChat.ts` |
| 流式对话后端 | 🟢 低 | 模板 `stream_chat` 端点 |
| 画像 Prompt 拼接 | 🟡 中 | 固化到 `profile_service.py` |
| CORS / 部署配置 | 🟡 中 | 人手工配置，不依赖 AI |
| **整体 AI 编码信心** | **~85-90%** | |

---

## 七、五份模板清单（AI 编码前必须产出）

按优先级排序，建议在 Day 1 业务代码之前全部生成完毕：

| # | 文件 | 行数 | 用途 |
|---|------|------|------|
| 1 | `frontend/src/lib/api.ts` | ~25 | 统一数据访问封装 |
| 2 | `frontend/src/hooks/useStreamChat.ts` | ~40 | 流式对话 hook |
| 3 | `backend/routers/chat.py` | ~30 | 流式对话端点 |
| 4 | `backend/auth/supabase_client.py` | ~15 | JWT 透传工厂函数 |
| 5 | `backend/db/pgmq_helpers.sql` | ~10 | pgmq 建队列+入队+消费 |

---

## 八、人机分工最终确认

| 🧑 人做 | 🤖 AI 做 |
|---------|----------|
| Supabase 项目创建 | `schema.sql` + `pgmq_helpers.sql` |
| Railway/Render 项目创建 | 所有 FastAPI 路由/服务/配置 |
| Vercel 项目创建 | 所有 React 页面/组件/hook/表单 |
| 环境变量粘贴（共 11 个） | 5 份模板文件 |
| 域名/SSL 配置 | `tailwind.config.ts` + `globals.css` |
| SQL 在 Supabase 执行 | TanStack Query hooks |
| 部署后全链路验证 | pytest 3 条关键链路测试 |
| — | README.md |

---

## 九、与上游文档的关系

本审查结论是对以下文档的技术栈章节的修订：

- **PRD v1.2**：第 1 节技术栈声明、第 15 节待确认问题
- **原型搭建计划 v1.2**：第二节技术栈、第五节 Agent 设计、第七节页面交互、第十四节技术决策

**建议**：
1. 将本审查结论上传至项目资料库，作为研发实现的技术依据
2. 将原型搭建计划中涉及数据访问路径、流式对话模板化的部分替换为本结论的对应章节
3. 通知技术负责人（如有）关注 R1-R4 四项修订

---

> **审查状态**：✅ 全部分支已完成追问，所有决策达成共识。
> 
> **下一步**：进入研发实现阶段，以本结论为技术基线，AI 产出全部代码。
